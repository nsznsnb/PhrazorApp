@page "/genres/create"
@page "/genres/edit/{Id:guid}"
@attribute [Authorize]

<PageTitle>@(IsEdit ? "フレーズジャンル編集" : "フレーズジャンル新規作成")</PageTitle>

<BackLink Href="/genres" />
<SectionTitle Title="@(IsEdit ? "フレーズジャンル編集" : "フレーズジャンル新規作成")" />

<GenreEditor Id="Id" IsDialog="false" />
@code {
    [Parameter] public Guid? Id { get; set; }

    private bool IsEdit => Id != null;
}