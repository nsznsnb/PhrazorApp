@page "/admin/settings/proverbs/edit/{id:guid?}"
@attribute [Authorize(Roles = "Admin")]

<MudStack Spacing="2">
    <SectionTitle Title="格言マスタ（編集）" />
    <BaseCard Class="pa-4">（実装予定）</BaseCard>
</MudStack>

@code {
    [Parameter] public Guid? Id { get; set; }
}
